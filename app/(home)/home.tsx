import { ScrollView } from "react-native";
import { Text } from "react-native-svg";

function Home() {
  return (
    <ScrollView>
      <Text>Hello Home</Text>
    </ScrollView>
  );
}

export default Home;
